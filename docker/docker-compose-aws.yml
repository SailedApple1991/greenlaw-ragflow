# ===================================================================
# RAGFlow AWS 部署专用 Docker Compose
# ===================================================================
# 只启动 ragflow，不启动 mysql/redis/minio/opensearch
# 这些服务由 AWS RDS/ElastiCache/S3/OpenSearch 提供
# ===================================================================

services:
  ragflow:
    image: ${RAGFLOW_IMAGE:-infiniflow/ragflow:dev}
    container_name: ragflow-server
    mem_limit: 13g
    cpus: 3.5
    ports:
      - "127.0.0.1:${SVR_WEB_HTTP_PORT:-80}:80"
      - "127.0.0.1:${SVR_WEB_HTTPS_PORT:-443}:443"
      - "127.0.0.1:${SVR_HTTP_PORT:-9380}:9380"
      - "127.0.0.1:${ADMIN_SVR_HTTP_PORT:-9381}:9381"
    volumes:
      - ./ragflow-logs:/ragflow/logs
      - ./nginx/ragflow.conf:/etc/nginx/conf.d/ragflow.conf
      - ./nginx/proxy.conf:/etc/nginx/proxy.conf
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./service_conf.yaml.template:/ragflow/conf/service_conf.yaml.template
    env_file: .env
    environment:
      - TZ=${TZ:-Asia/Singapore}
      - DOC_ENGINE=opensearch
    networks:
      - ragflow
    restart: on-failure
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  ragflow:
    driver: bridge
